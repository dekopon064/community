# PWA 아이콘

이 폴더에 아래 두 개의 PNG 파일을 넣으면 `app/manifest.ts`가 자동으로 인식합니다.

- `icon-192x192.png` (192×192)
- `icon-512x512.png` (512×512)

두 아이콘은 홈 화면 추가/설치 시 사용되며, `maskable` 용도로도 매핑되어 있으므로
아이콘 가장자리에 약간의 여백(safe zone)을 두는 것을 권장합니다.
